import { describeComponent, createMockStore } from 'test';
import $NAME$Connector from './$FILE_NAME$-connector';
import $NAME$ from './$FILE_NAME$';

describeComponent($NAME$Connector, subject => {
  let store;

  beforeEach(() => {
    store = createMockStore();
  });

  it('', () => {

  });

  describe('', () => {

  });
});
