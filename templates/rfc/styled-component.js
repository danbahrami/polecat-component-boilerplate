import React, { PropTypes } from 'react';
import styles from './$FILE_NAME$-style.scss';

const $NAME$Component = (props) => {
  return (
    <div/>
  );
};

$NAME$Component.propTypes = {};

export default $NAME$Component;
